---
order: 7168
title: [feat][to be developed]fallback agent
status: To Do
---
When usage of an agent is consumed, make the app switch to a fallback agent. This needs to be configurable in app level settings (default) and workspace’s settings. Also add it to the workspace init wizard

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
