---
order: 2048
kind: task
title: [feat] rails clear all
status: Done
---
In orchestration tab, add a "Clear done" button next to "Run all". It should (after user confirm via propetary prompt) remove all finished rails (rails with ALL steps done).

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
