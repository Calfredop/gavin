---
order: 3072
kind: task
title: [feat] tool editing
status: Done
---
Allow tool editing in hub’s tool tab.
Allow tool type switch in tool edit modal (to let them be used in orchestration too).

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
