---
order: 6144
kind: task
title: [feat] hub tabs sorting/hide
status: Done
---
Allow workspace hub tabs sorting by dnd, like we do for page's tabs. This should be locked by default and activated/deactivated by a "smaller than tabs" icon button (with tooltip) at the end of the tabs.
In app (as default) and workspace's settings allow to manage wich hub sections are activated, via a modal with an eye/crossed eye action for each hub item.

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
