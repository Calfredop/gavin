---
order: 2048
kind: task
title: [feat] task manager improvements
status: Done
---
- make the list of sessions in a grid, sortable by name, mem and state (active, idle, stale, hidden...) and with multiple select option (shift and cmd/ctrl, show hint somewhere like we do for the new card modal keyboard shortcut)
- add a clear stale button
- make the kill all button ask for a confirm before killing
- kill all does nothing
- make the task manager header and footer sticky so that the grid scroll inside the height of the task manager body

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
