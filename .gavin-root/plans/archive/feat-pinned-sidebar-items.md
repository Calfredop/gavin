---
order: 1024
kind: task
title: [feat] pinned sidebar items
status: Done
---
Allow (via contextual menu) to pin workspaces and pages inside workspaces; they cannot be closed (remove X action) and stays on top (timestamp order if multiple)

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
