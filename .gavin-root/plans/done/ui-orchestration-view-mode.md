---
order: 11264
kind: task
title: [ui] orchestration view mode
status: Done
complexity: intricate
---
Add a view mode switcher next the search input in Orchestration tab. It should allow to switch from the current vertical rails view, to a nodes view. The nodes view should have each step have it’s node; each node type have it’s icon; each rail have it’s color; if a step starts a rail an arrow to the next rail should be shown; groups should also be shown differentiating in ui by sequence and pararellel type

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
