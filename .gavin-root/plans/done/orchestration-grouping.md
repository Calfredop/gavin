---
kind: task
title: Orchestration grouping
status: Done
---
In orchestration when dropping a card onto another, current behaviour is to transform the step into a parallel one. I want this changed: it should transform the step into a sequential group the user can move around, and optionally switch to parallel. Groups formed like this should also be storable as workspace/global templates (eg. merge + push).
