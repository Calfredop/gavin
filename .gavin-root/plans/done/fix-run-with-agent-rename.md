---
order: 10240
kind: task
title: [fix] run with agent rename
status: Done
---
rename the run with "agent" quick button on cards to be "hub", and the "session" to "new session"

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
