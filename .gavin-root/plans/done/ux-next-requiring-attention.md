---
order: 13312
kind: task
title: [ux] next requiring attention
status: Done
---
In top bar, before the add new page button, add a next icon button, with dropdown (just like the add); it should allow to navigate to sessions needing attention (waiting for user prompt). If they are all clear, disable it

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
