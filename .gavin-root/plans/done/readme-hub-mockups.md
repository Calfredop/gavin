---
title: Show the hub's Kanban and Orchestration tabs in the README
status: Done
complexity: simple
---
The README's only image is the agents view, but the workspace hub — the Kanban board and the Orchestration rails — is where the app's work actually happens. Add invented-data mock-ups of both, drawn from the real markup and theme tokens rather than guessed.

- [x] Read the Kanban and Orchestration surfaces (markup + CSS) into a visual spec
- [x] Draw `docs/images/mock-kanban.svg`
- [x] Draw `docs/images/mock-orchestration.svg`
- [x] Rework the README so the hub leads and the agents view supports it
- [x] Pre-flight: every image path resolves, SVGs render the same in rsvg and Chrome, no real project names
