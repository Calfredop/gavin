---
order: 2048
kind: task
title: [feat] agents parsing
status: Done
---
During workspace init, do a sweep of compatible agents to see what are present and integrate them into wizard as ‘found’ and allowing the user to set them as main and fallback (checking against the app settings too).

<!-- gavin:auto-commit -->
When the implementation is done, commit it. Commit only the files you touched — never `git add -A`. Do not push.
<!-- /gavin:auto-commit -->
